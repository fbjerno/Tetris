instead of using SDL_GeyKeyboardState(), set bool movingX to -1/1 when left/right pressed, and 0 when released
add small timer that makes moving faster after maybe 100ms, so you can be precise when you need to, but fast otherwise
draw outline of tetromino where it will land
points when 
score based on combos
flashing (vfx), sfx
wall kicks
losing

(maybe)
scoreboard
